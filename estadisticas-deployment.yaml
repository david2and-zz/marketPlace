apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: marketPlace-deployment
  labels:
    app: marketPlace-deployment
spec:
  replicas: 3 # tells deployment to run 3 pods matching the template
  template:
    metadata:
      labels:
        app: marketPlace-deployment
        tier: backend
        stack: akka
        serves: http
    spec:
      containers:
        - name: marketPlace-microservice
          image: gcr.io/heroic-alpha-195822/a2censo-estadisticas
          ports:
            - containerPort: 5000
		  resources:
          requests:
            cpu: 100m
            memory: 100Mi
          env:
            - name: GET_HOSTS_FROM
              value: 127.0.0.1:3306
            # These secrets are required to start the pod.
            # [START cloudsql_secrets]
            - name: A2CENSO_DB_USER
              valueFrom:
                secretKeyRef:
                  name: cloudsql-db-credentials
                  key: username
            - name: A2CENSO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cloudsql-db-credentials
                  key: password
            # [END cloudsql_secrets]
        # [START proxy_container]
        - name: cloudsql-proxy
          image: gcr.io/cloudsql-docker/gce-proxy:1.11
          command: ["/cloud_sql_proxy",
                    "-instances=heroic-alpha-195822:us-central1:a2censo=tcp:3306",
                    "-credential_file=/secrets/cloudsql/credentials.json"]
          volumeMounts:
            - name: cloudsql-instance-credentials
              mountPath: /secrets/cloudsql
              readOnly: true
        # [END proxy_container]
      # [START volumes]
      volumes:
        - name: cloudsql-instance-credentials
          secret:
            secretName: cloudsql-instance-credentials
        - name: cloudsql
          emptyDir:
      # [END volumes]
---
apiVersion: v1
kind: Service
metadata:
  name: mp-microservice-akka-service
spec:
  type: LoadBalancer
  selector:
    app: marketPlace
    tier: backend
    serves: http
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000